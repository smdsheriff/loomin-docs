# Never copy host dependencies into the image — they are installed by `npm ci`
# in the builder stage, and host-platform binaries would break the Alpine build.
node_modules
dist

# Local tooling and editor state
.vscode
.idea
*.swp
*.log
npm-debug.log*

# Environment
.env
.env.*
!.env.example

# Docker
Dockerfile
.dockerignore

# OS
.DS_Store
Thumbs.db
